# Server environment (copy to .env and fill in)
NODE_ENV=development
PORT=5000

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/devhub

# CORS origin (the Next.js client)
CLIENT_URL=http://localhost:3000
